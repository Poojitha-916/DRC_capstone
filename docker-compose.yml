services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: gscholar
      POSTGRES_PASSWORD: gscholar
      POSTGRES_DB: gscholar
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data

  app:
    build: .
    environment:
      DATABASE_URL: postgres://avnadmin:AVNS_rS7PrxTF3ZigYE2NWYg@drcpg-311b7b85-drc123.g.aivencloud.com:20040/defaultdb?sslmode=require
      SESSION_SECRET: gscholar-hub-secret-key-2024
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
    command: npm run dev

volumes:
  db-data:
